---
description: Example custom slash command
argument-hint: "[optional argument]"
---

This is an example custom slash command. Replace the body with the prompt you want Claude to execute when the user runs `/example`.

Arguments passed to the command: $ARGUMENTS
